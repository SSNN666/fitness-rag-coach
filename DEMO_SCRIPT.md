# 🎬 面试演示剧本（6 步主流程，约 6-8 分钟）

> 原则：**每个动作都要有目的**——你点开的每个东西都对应一个可讲的点。
> 演示前把每个问题都实际跑一遍（伤病问题检索有波动，提前验证命中）。

## 0. 准备清单（演示前 15 分钟）

- [ ] `python start.py`（或 `--no-browser`）——确认 API + UI 都起来
- [ ] 浏览器 **Ctrl+F5** 强刷（确保最新脚本——流式无光标版本）
- [ ] 侧边栏填画像：身高 175 / 体重 75 / 目标减脂 → **保存**
- [ ] 把下方"备选问题"全部实际问一遍，确认每个都有好回答
- [ ] 网络通畅（云端模型）；投屏/窗口已就绪
- [ ] 确认 `curl localhost:8000/healthz` 返回 `"chat_chain":["dashscope",...]`（主链是云端）

开场一句话（30s）：
> "这是一个康养知识库 RAG 问答系统：FastAPI 后端 + Streamlit SSE 客户端，三路检索
> （Milvus 向量 + BM25 关键词 + 图谱可插拔），12 步安全流水线，多供应商降级链。"

---

## 1. 快速问答 —— 展示速度与分层策略（~8s）

**输入**：`深蹲主要锻炼哪些肌群`

**看点**：
- 纯文本流式输出（无光标、无卡顿）
- 回答后的 **📚 参考来源** 展开器（杠铃深蹲 / 高脚杯深蹲…）
- **🔍 检索详情（调试）** 展开器：每篇文档的来源 + score（当场展示中间过程）
- 底部 token 统计 caption（`⚙️ tokens: xx | provider: dashscope | 模式: ⚡ 快速`）

**话术**：
> "这是 simple 层：快模型 qwen3.7-flash、跳过 HyDE 和 LLM 重排，首 token 约 1.5 秒、
> 全程不到 3 秒（实测压测表：1 并发总延迟 2.7s）。检索详情里能看到三路融合后每篇文档的得分。"

**救场**：若命中差 → 换 `平板支撑主要锻炼什么`。

---

## 2. 伤病问答 —— 展示分层切换 + Fact-Check + 图谱入口（~15s）

**输入**：`腰突怎么康复`

**看点**：
- 回答是**三部分结构**：①知识库依据（带具体动作名）②推断（标注"因人而异"）③就医提示
- 底部出现 **🕸️ 查看伤病关系图谱** 按钮 → 点击 → 跳图谱视图并自动聚焦"腰突"
- 回答末尾 **💡 深度思考提示**

**话术**：
> "这是 injury 层：主模型 + LLM 重排 + Fact-Check 四类校验（动作/康复周期/负重/禁忌），
> 校验失败会触发 CRAG 联网修正。回答结构是 Prompt 强制的——知识库有依据的部分、
> 推断的部分（明确标注）、需要就医的部分，三块分开，防编造。"

**救场**：图谱按钮依赖伤病实体命中，若没出现 → 换 `膝内扣怎么改善`。

---

## 3. 多轮改写 + 禁忌拦截 —— 展示安全防线（~10s）★核心亮点

**输入**：`那硬拉呢`

**看点**：
- **直接拒绝**，文案：`「硬拉」属于腰间盘突出的明确禁忌动作…禁忌原因：硬拉需弓背发力，直接压迫腰椎`
- 无回答、无检索——安全闸门在生成前

**话术**（这段要背熟）：
> "第二轮指代问句会被自动改写成『腰突患者可以做硬拉吗』——改写只作用于检索。
> 但注意：改写后实体合并回安全管线，禁忌黑名单重新生效，所以硬拉（腰突的明确禁忌）
> 在生成前就被边界拒绝拦下。这层是我自测时发现的漏洞——多轮改写会把伤病上下文带进
> 检索，却绕过了禁忌检查——我修复并加了回归测试（tests/test_pipeline.py 里的
> test_rewrite_contraindication_intercepted）。"

**可选延伸**（时间充裕时）：输入 `那臀桥呢` → 正常回答，打开检索详情，
**query 显示为改写后的完整问题**（"腰突患者可以做臀桥吗"）——改写的可视化证据。

---

## 4. 深度思考开关 —— 展示预算与分层（~40s，可选）

**动作**：侧边栏勾选 **深度思考模式** → 重问 `腰突怎么康复`

**看点**：回答更长更深 + token caption 显著增长（对比快速模式）

**话术**：
> "默认复杂层用 plus 关思考，约 17 秒；开启深度思考后切思考模式，约 40 秒、token 翻倍。
> 这是分层生成策略的预算控制——简单问题不被慢模型拖住，复杂问题可以深入。"

---

## 5. 健康工具 —— 展示确定性计算（~8s）

**输入**：`帮我算下BMI`（画像已保存：身高 175 / 体重 75）

**看点**：
- 回答直接出现 `BMI=24.5，属超重`
- **参考来源** 里有一条 `kind=tool` 的 **BMI 计算** 条目（带完整计算式）

**话术**：
> "健康数值不让 LLM 算——BMI 这类计算必须确定性、可复现、零 token 成本。
> 参数从画像抽取（身高体重），缺参数不硬算。注册表结构，将来可以直接接 tools 协议
> 让模型自主调用。"

**救场**：若回答没引用工具结果 → 检查画像是否已保存（侧边栏要看到"✅ 身体数据已保存"）。

---

## 6. 反馈闭环 —— 展示产品意识（~10s）

**动作**：对刚才的回答点 **👎**（或 👍）→ 出现 toast

**看点**：`feedback.jsonl` 里多了一行（问题 + 回答 + 评价 + 时间戳）

**话术**：
> "反馈落盘 feedback.jsonl，评测脚本 `python eval_testset.py --feedback` 会把负反馈问题
> 回流评测——检索诊断 + faithfulness/relevancy。测试集不是一次性构建的，
> 真实用户不满意的地方会驱动下一轮迭代。"

**收尾一句话（30s）**：
> "整个系统单机可跑、143 个测试全过、CI 在 GitHub Actions 上绿了三轮。
> 我知道单机约束的边界在哪——Milvus Lite 单进程、内存会话、进程内限流，
> 上多实例这三处要换独立服务。"

---

## 面试官打断预案（问什么答什么，不要展开讲别的）

| 追问 | 答案要点 |
|---|---|
| 为什么拒答硬拉？ | 边界拒绝：实体→禁忌黑名单（contra_data 28 类，图谱/本地双源）→ 核心诉求=禁忌动作直接拒 |
| 改写用哪个模型？成本？ | hyde 角色小模型（0.5B / flash），max_tokens=64，异常/空输出回退原问题 |
| BMI 为什么不走 tools 协议？ | 健康计算必须确定性（LLM 算术不可靠）；注册表结构已为 tools 协议留好路径 |
| 反馈数据怎么用？ | eval_testset.py --feedback：负反馈问题回流评测（无金标准→检索诊断 + faithfulness/relevancy） |
| 检索详情数据从哪来？ | gateway.log JSONL 结构化日志，pipeline 每请求写一次 retrieval 事件 |
| 三个深度问题必背 | ①GROUNDING_MIN_SIM=0.40 是 qwen 嵌入实测校准（无关类 max 0.29-0.38）②RRF 无阈值语义 vs weighted 有 ③权重 0.40/0.30/0.30 给图谱让空间 ④enable_thinking 只 DashScope 认识，下发 DeepSeek/千帆会被 400 拒绝 |

## 备选问题库（防现场翻车）

| 场景 | 问题 | 预期 |
|---|---|---|
| simple 层 | `平板支撑主要锻炼什么` / `硬拉的动作要领` | 快答 + 引用 |
| injury 层 | `膝内扣怎么改善` / `网球肘能练卧推吗`（后者可能触发禁忌拒绝——也算看点） | 三部分结构 |
| 工具 | `我今年30岁运动心率多少合适`（无画像也有年龄参数） | 心率区间 tool 引用 |
| 改写 | `那平板支撑呢`（接腰突轮次） | 正常回答 + 改写 query 证据 |
| 拒答（grounding） | `如何挑选股票` | 语义相关度 0.00 → 生成前拒答 |
| 弱相关问题 | `量子力学和健身的关系` | **不拒答**：含「健身」故相关度 0.52 > 阈值 0.40 → 如实说明无直接关联 |

> ⚠️ **修正记录（2026-09-10）**：本表原写「量子力学和健身的关系 → 知识库无依据拒答」，
> 但**实测该问题不会被拒答**——它含「健身」，与知识库的语义相关度 0.52，高于
> `GROUNDING_MIN_SIM=0.40`，系统判定「有相关材料」并如实回答「无直接关联，建议聚焦科学健身」。
> 这个行为是**对的**（比拒答更有用），错的是这句没实测过的文档声明。
> 演示拒答请改用 `如何挑选股票`（相关度 0.00）。

## 反模式（不要做）

- ❌ 不问背景直接演示——开场 30 秒的架构一句话很重要
- ❌ 演示中途开多个问题并行——每步演示一个点，完成再说下一句
- ❌ 被追问时解释"这是 demo"——主动说边界（"我知道单机约束在哪"）
- ❌ 在"那硬拉呢"之前就打开过检索详情——证据感靠顺序铺垫
