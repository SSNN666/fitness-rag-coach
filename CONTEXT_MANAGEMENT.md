# 🧠 上下文管理设计

> 本文说明「一次请求里，什么东西能进模型、进多少、超了怎么裁」。
> 相关代码：[gateway.py](gateway.py) 的 `_TokenBudgetGuard`、[pipeline.py](pipeline.py) 的 `_active_provider`、[config.py](config.py) 的预算常量。

---

## 为什么需要它

一次请求要同时塞进四样东西：

| 组成 | 量级 | 特点 |
|---|---|---|
| system 提示 | ~500-1500 tokens | 固定，不可裁 |
| 会话历史 | 累积增长 | 可裁（丢最早的） |
| **禁忌黑名单** | ~200-800 tokens | **绝对不能裁**（安全数据） |
| 检索上下文 | 变动大 | 可裁（丢尾部） |
| 用户问题 | ~50-600 tokens | 不可裁 |
| **答案预留** | ≥500 tokens | 必须留够，否则答案中途截断 |

**不加管理会出两类事故**：
1. **超出模型窗口** → 请求直接失败（或模型静默截断输入，答案答非所问）
2. **答案没空间** → 生成到一半被硬停，回答断在句中间

---

## 一、预算怎么定：按**实际激活的供应商**取窗口

```python
budget = ctx_window(provider) × CONTEXT_BUDGET_RATIO(0.55)
```

| 供应商 | 上下文窗口 | 预算 |
|---|---|---|
| dashscope（qwen3.7-plus/flash） | 131072 | **72089** |
| deepseek | 65536 | 36044 |
| qianfan（ernie-4.5-turbo-128k） | 131072 | **72089** |
| ollama（本地） | 8192 | 4505 |
| 未知 / 取不到 | 8192（安全回退） | 4505 |

**⚠️ 这里修过一个 bug**：原实现写死 `ollama_num_ctx × 0.55`——**恒按本地 8192 算**。云端主链可用窗口是它的 16 倍，却被按本地的限制裁，检索到的内容被无谓截断。这不是「保守」，是**拿本地模型的约束去限制云端模型**。修复见 `_TokenBudgetGuard._resolve_ctx_window`，并有回归护栏测试（`test_cloud_budget_is_substantially_larger`）。

> **为什么用 0.55 而不是 1.0**：预算只覆盖「输入」部分。模型还要留空间生成输出，且不同供应商对输入+输出的总长度限制不同，留 45% 余量是保守但安全的选择。

---

## 二、超预算了怎么裁：级联截断

**顺序不能反**——先裁最不重要的：

```
① 裁会话历史（保留最近 ≥2 条）
      ↓ 仍超预算
② 裁检索上下文尾部
```

### ① 历史裁剪：保留最近 ≥2 条

`CHAT_MAX_HISTORY_TOKENS = 2048`。从**最早**的消息开始丢，但**至少保留 2 条**（一轮完整的 user+assistant）——少于这个数，多轮指代消解就没有依据了，历史等于白存。

### ② 上下文尾部截断：**保留头部**

这是整块设计里最关键的一条：

> **禁忌黑名单固定在 context 头部**（pipeline 注入时就是往头部拼），**截断只动尾部**。

原因：黑名单是**安全数据**。如果它被裁掉，模型就不知道哪些动作是禁忌的——**安全防线会随着上下文变长而静默失效**。把不可裁的东西放头部、可裁的放尾部，是最简单的保证方式。

截断按**句子边界**切（`[。！？；\n]`，且不在 `」』）)` 前切），不切断句子中间。

**又修过一个 bug**：原实现 `split` 后 `"。".join()` 重组——而 `split` 已经吃掉了分隔符，所以 `！？；` 和**换行全部被替换成「。」**。换行尤其要紧：黑名单是按 `\n` 分条的，被压成一行跑文后模型可读性明显下降。现改为捕获组 split + 原样拼回。

**兜底**：如果连一个句子都放不下（超长无标点文本），退化为字符级截断并加 `…`。

---

## 三、分层预算：按查询复杂度分配生成空间

检索预算是一回事，**生成**预算又是另一回事（见 [config.py](config.py) 的 `LLM_TIERS`）：

| 层 | 判定 | 模型 | 生成预算 | 重排 | 实测耗时 |
|---|---|---|---|---|---|
| simple | 无伤病/体态/计划关键词 | qwen3.7-flash | 400 + 「200字内」 | 跳过 | ~8s |
| injury | 含伤病/体态实体 | qwen3.7-plus（关思考） | 1024 + 「600字内」 | ✓ | ~17s |
| plan | 含计划类关键词 | qwen3.7-plus（关思考） | 2048 | ✓ | 按长度 |

**设计意图**：简单问题不该被慢模型和长预算拖住，复杂问题也不该被短预算截断。分层判定发生在**检索之前**，所以 simple 层能顺带跳过 HyDE 和重排，省两次 LLM 调用。

---

## 四、可观测性

超预算时 `guard()` 返回 `budget_info`：

```python
{
  "total_before": 8000,
  "total_after": 4500,
  "budget": 72089,
  "action": "trim_history" | "trim_context" | "trim_both",
  "show_warning": True    # 截断比例 ≥ 30% 时置位，前端展示告警
}
```

同时写结构化日志 `token_budget_trim`（警告级），含截断前后总量与比例——线上能直接看出**是「本来就没检索到东西」还是「检索到了但被裁了」**。

---

## 五、边界与已知限制

| 点 | 说明 |
|---|---|
| **token 估算不是精确值** | 用 `CHAT_TOKEN_ESTIMATE_RATIO=1.8`（中文字符×1.8≈token）粗略估算，不引入 tiktoken 依赖。**宁可估高**（提前截断好过超限失败）。 |
| 截断会**修改真实会话存储** | 历史裁剪是就地生效的（`session.clear()` + 重放），不是临时副本——所以裁剪结果会被持久化。 |
| 单机适用 | 预算计算在进程内，多实例需要把会话存储换成共享存储（`SessionStore` 的 dict 接口即替换点）。 |
| 字符级截断是**下策** | 只在「一整段无标点」时触发，这种情况本身就说明文本质量有问题（OCR 乱码等）。 |

---

## 🎤 面试怎么说

> 「上下文管理我主要解决两个问题：**别超模型窗口**、**别把答案空间挤没**。
>
> 预算是按供应商取的——这里我修过一个 bug：原来写死了本地的 8192，等于拿本地小模型的限制去裁云端 128k 的上下文，白扔 16 倍窗口。
>
> 裁的时候是级联的：先裁会话历史（保留最近 2 条，少于这个多轮指代就没依据了），还不够就裁检索上下文尾部。
>
> **最关键的一条是：禁忌黑名单固定在上下文头部，截断只动尾部。** 因为它是安全数据——如果被裁掉，模型就不知道哪些动作禁忌，**安全防线会随着对话变长而静默失效**。把不可裁的放头部、可裁的放尾部，是最简单的保证方式。
>
> 这块我也修过一个内容失真的 bug：截断后重组时，我的实现把原标点全替换成了句号——包括换行。而禁忌黑名单是按换行分条的，被压成一行跑文后模型可读性明显下降。**这种 bug 不会报错，只会让回答质量悄悄变差。**」
